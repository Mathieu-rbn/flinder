function refreshFlatList(innerHTML) {
  const newFlatList = document.getElementById('flats_partial_show');
  newFlatList.innerHTML = "";
  newFlatList.innerHTML = innerHTML;
}

  refreshFlatList('<%= j render "viewings/flats_list", flats: @flats %>');














/*if @viewing.matched?

      redirect_to flats_path, notice: "you have a match!"

      else
         redirect_to flats_path
  end*/
