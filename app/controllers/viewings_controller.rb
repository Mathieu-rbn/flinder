class ViewingsController < ApplicationController
  def new
    # @viewing = Viewing.create(user_id: current_user.id, flat_id: params[:flat_id], like: true)
  end
end

# figure out if the viewing matches of the previus viewing if save
